framework:
    validation:
        not_compromised_password: false
        enabled: true
        enable_annotations: true
        mapping:
            paths:
                - "%kernel.project_dir%/config/validator/"
    annotations: true
